[pytest]
# 기본 수집 대상은 tests/ 만. 루트의 test_dart_connection.py / test_gemini_503.py 는
# 네트워크가 필요한 진단 스크립트이므로 기본 회귀 실행에서 제외한다.
testpaths = tests
pythonpath = .
markers =
    network: 실제 DART/Gemini 네트워크 호출이 필요한 테스트 (기본 회귀에서 제외)
addopts = -q
