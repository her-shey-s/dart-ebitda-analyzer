# DART EBITDA Analyzer - 기능명세서

## 1. 프로젝트 개요

DART(전자공시시스템) API를 활용하여 한국 기업의 재무 데이터를 자동 추출하고, EBITDA 계산에 필요한 항목까지 포함하여 검증된 재무 정보를 제공하는 웹 애플리케이션이다.

- **대상 기업**: 상장사 + 비상장 외감기업 (DART에 공시가 존재하는 모든 기업)
- **대상 연도**: 2020 ~ 2025년
- **출력 형식**: 웹 대시보드 (Streamlit) + Excel 다운로드
- **기술 스택**: Python, Streamlit, BeautifulSoup, Google Gemini API

---

## 2. 추출 항목

### 2.1 재무상태표 (BS)

| 항목명 | DART 코드 | 매칭 키워드 |
|--------|-----------|-------------|
| 총자산 | `ifrs-full_Assets` | 자산총계, 총자산 |
| 총부채 | `ifrs-full_Liabilities` | 부채총계, 총부채 |
| 자본총계 | `ifrs-full_Equity` | 자본총계, 총자본 |
| 이익잉여금 | `ifrs-full_RetainedEarnings` | 이익잉여금, 이익잉여금(결손금), 결손금, 미처분이익잉여금 |

### 2.2 손익계산서 (IS)

| 항목명 | DART 코드 | 매칭 키워드 | 특수 처리 |
|--------|-----------|-------------|-----------|
| 매출액 | `ifrs-full_Revenue` | 매출액, 영업수익, 수익(매출액), 매출수익 | - |
| 매출원가 | `ifrs-full_CostOfSales` | 매출원가, 매출액의원가, 제품매출원가, 상품매출원가 | - |
| 매출총이익 | `ifrs-full_GrossProfit` | 매출총이익, 매출총이익(손실), 매출총손실 | - |
| 영업이익 | `dart_OperatingIncomeLoss` | 영업이익, 영업이익(손실), 영업손실, 영업손익 | "영업손실" 매칭 시 부호 반전, "계속영업/중단영업" 제외 |
| 당기순이익 | `ifrs-full_ProfitLoss` | 당기순이익, 당기순이익(손실), 당기순손실, 연결당기순이익 등 | "순손실" 매칭 시 부호 반전, "계속영업/중단영업" 제외 |

### 2.3 감가상각비 / 무형자산상각비 (EBITDA용)

| 항목명 | 추출 소스 | 비고 |
|--------|-----------|------|
| 감가상각비 | 현금흐름표(CF) 영업활동 조정항목, 주석(NOTES) | 전용 추출기 사용 |
| 무형자산상각비 | 현금흐름표(CF) 영업활동 조정항목, 주석(NOTES) | 전용 추출기 사용 |

> EBITDA = 영업이익 + 감가상각비 + 무형자산상각비

---

## 3. 데이터 추출 경로

입력된 기업에 대해 보고서 유형에 따라 두 가지 경로 중 하나로 자동 분기된다.

### 3.1 경로A: DART 재무제표 API (사업보고서)

- **대상**: 사업보고서가 존재하는 기업 (주로 상장사)
- **API**: `fnlttSinglAcntAll.json` (단일회사 전체 재무제표)
- **매칭 방식**:
  1. 1차: `account_id` (IFRS 코드) 정확 일치
  2. 2차: `account_nm` (한글 계정명) 키워드 포함 일치 (폴백)
- **재무제표 기준**: 연결(CFS) 우선, 없으면 별도(OFS)

### 3.2 경로B: DART XML 문서 파싱 (감사보고서)

- **대상**: 사업보고서가 없는 기업 (비상장 외감기업 등)
- **보고서 우선순위**: 연결감사보고서 > 감사보고서
- **API**: `document.xml` (보고서 원문 ZIP 다운로드)
- **파싱 방식**:
  1. ZIP 내 XML 파일 추출 (가장 큰 파일 선택)
  2. BeautifulSoup으로 DART XML(dart4.xsd) 파싱
  3. `<TITLE>` 태그 기반 섹션 분류 (재무상태표/손익계산서/현금흐름표/주석)
  4. 테이블 행에서 키워드 매칭으로 항목별 금액 추출
- **항목명 정규화**: 주석 참조 제거, 전각 공백 제거, 로마자/아라비아/한글 목차 번호 제거

### 3.3 보고서 탐색 우선순위

```
1. 사업보고서 (11011) → 경로A
2. 연결감사보고서 (11004) → 경로B
3. 감사보고서 (11005) → 경로B
```

---

## 4. 감가상각 전용 추출기

감가상각비와 무형자산상각비는 재무항목 추출과 독립된 전용 모듈로 추출한다.
재무항목 추출 결과와 무관하게 동작하며, 실패해도 다른 항목에 영향을 주지 않는다.

### 4.1 추출 워크플로우

```
[1차] 현금흐름표(CF) → Python 키워드 매칭 (AI 불필요, 가장 신뢰도 높음)
  ↓ 못 찾은 항목이 있으면
[2차] 주석(NOTES) → Python 키워드 매칭 + AI 교차검증 (Gemini)
  ↓
CF 결과와 주석 결과 병합 (CF 우선)
```

### 4.2 추출 규칙

- **전체 비용 기준** 값만 선택 (회사 전체 기준)
- 판매비와관리비, 기능별 배분, 특정 자산별 감가상각은 부분값이므로 제외
- 감가상각누계액(누적값)은 당기 비용이 아니므로 제외
- 이연법인세 관련 감가상각비는 다른 맥락이므로 제외
- 단위 자동 감지: 원/천원/백만원/억원 → 원(KRW) 단위로 변환

### 4.3 합산 공시 처리

"감가상각비 및 무형자산상각비"로 합산 표기된 경우:
- 감가상각비 란에 합산액 기입
- 무형자산상각비는 None (분리 불가)
- 비고에 합산 사실 기록

### 4.4 연결/별도 범위 구분

사업보고서(경로A)에는 연결/별도 주석이 모두 포함되므로:
- `fs_div`에 따라 올바른 주석 섹션에서만 테이블 수집
- 연결재무제표 주석 / 별도재무제표 주석 경계를 `<TITLE>` 태그로 판별

---

## 5. AI 교차검증

### 5.1 재무항목 AI 검증 (경로A/B 공통)

Python 파싱 결과를 Gemini가 독립 추출한 결과와 비교하여 정확도를 높인다.

```
[AI 1회차] 재무제표 원문에서 9개 항목 독립 추출
  ↓
Python 결과와 비교 (상대오차 0.1%, 절대오차 1원 기준)
  ↓ 불일치 항목 존재 시
[AI 2회차] 원문 + 양쪽 결과를 주고 판정 요청 (A/B 익명 표기로 편향 방지)
  ↓
최종 결과 결정
```

- **결과 상태**:
  - `agreed`: Python과 AI 일치 → Python 결과 채택
  - `adjudicated`: AI 판정으로 불일치 해소
  - `ai_extract_only`: AI 판정 실패, AI 1회차 결과 사용
  - `python_fallback`: AI 호출 자체 실패, Python 결과 사용

### 5.2 감가상각 AI 검증

주석에서 추출할 때만 사용. Python 추출 결과와 AI 추출 결과를 교차검증한다.

- AI가 테이블/행 번호를 인용하여 출처를 남기도록 프롬프트 설계
- Python이 분리 기재 쌍을 찾았는데 AI가 합산 공시를 선택한 경우 Python 결과 우선

### 5.3 AI 모델 및 Rate Limit

- **모델**: `gemini-3.1-flash-lite-preview`
- **Rate Limit**: 15 RPM (분당 최대 호출), 한도 도달 시 30초 대기
- **API 키**: 환경변수 `GEMINI_API_KEY` 또는 Streamlit Secrets

---

## 6. 데이터 검증

추출된 재무 데이터에 대해 회계 항등식 검증을 수행한다.

### 6.1 검증 규칙

| 규칙 | 수식 | 심각도 | 허용 오차 |
|------|------|--------|-----------|
| 회계항등식 | 총자산 = 총부채 + 자본총계 | critical | max(1원, 0.01%) |
| 매출총이익 항등식 | 매출총이익 = 매출액 - 매출원가 | critical | max(1원, 0.01%) |
| 영업이익 논리검증 | 영업이익 ≤ 매출총이익 | warning | - |

### 6.2 검증 결과

- `is_valid`: critical 실패가 없으면 `True`
- 필요 항목이 None이면 해당 검증은 스킵 (데이터 부족은 오류가 아님)
- 각 검증 항목별 기대값, 실제값, 차이, 통과 여부, 심각도를 상세 기록

---

## 7. 캐싱

### 7.1 결과 캐시

- **저장소**: SQLite (`cache.db`)
- **캐시 키**: `{corp_code}:{year}`
- **TTL**: 48시간
- **동작**: 분석 전 캐시 확인 → 히트 시 즉시 반환, 미스 시 전체 파이프라인 실행 후 저장

### 7.2 기업코드 캐시

- **저장소**: 메모리 → 파일 (`data/corp_codes.pkl`, 24시간 TTL) → DART API
- 3단계 캐싱으로 API 호출 최소화

---

## 8. 프론트엔드 (Streamlit UI)

### 8.1 입력

- **기업명 입력**: 텍스트 영역, 줄바꿈으로 여러 기업 입력 (최대 100개)
- **연도 선택**: 시작/끝 연도 드롭다운 (2020~2025, 기본 2022~2025)
- **캐시 사용**: 체크박스 (기본 ON)
- **캐시 초기화**: 버튼

### 8.2 분석 실행

- 입력된 기업명 x 선택된 연도의 모든 조합을 순차 처리
- 진행 표시줄 + 현재 처리 중인 기업/연도 표시

### 8.3 결과 표시

#### 요약 탭
- 전체 결과 테이블 (기업명, 연도, 재무제표기준, 경로, 보고서, 각 항목 억원 단위, 검증 상태)
- 검증 실패 행 빨간 배경 강조
- 집계 메트릭: 총 분석 건수, 추출 성공, 검증 통과, 검증 실패

#### 상세 탭
- 기업·연도별 접이식(expander) 상세 뷰
- 처리 상태 설명
- 검증 요약 (규칙별 기대값/실제값/차이/통과 여부)
- AI 비교 결과 (Python vs AI 불일치 내역)
- 재무 항목 상세 (항목명, 구분, 억원/원 단위 금액)
- 감가상각 추적 로그 (다운로드 가능)

### 8.4 다운로드

#### Excel 파일 (`dart_재무데이터.xlsx`)
- **회사별 시트**: 가로=연도, 세로=계정항목, 억원 단위, 연결/별도 + 경로 표시
- **원본 시트**: 원(KRW) 단위, 전체 메타데이터 포함 (corp_code, 경로, 보고서명, 처리상태, 검증상세, AI 비교결과 등)

#### 분석 로그 (`.txt`)
- 타임스탬프 포함 전체 분석 과정 로그

#### 감가상각 추적 로그 (`.txt`)
- 기업별 감가상각 추출 과정의 상세 추적 로그 (개별 다운로드)

---

## 9. 처리 상태 코드

| 상태 코드 | 의미 | 발생 조건 |
|-----------|------|-----------|
| `ok` | 정상 추출 완료 | 재무 데이터 추출 + 검증 완료 |
| `no_corp` | 기업 미발견 | DART 기업코드 목록에서 정확 일치하는 기업명 없음 |
| `ambiguous_corp` | 기업명 중복 | 동일 기업명이 2개 이상 등록되어 특정 불가 |
| `no_report` | 보고서 없음 | 해당 연도의 사업보고서/감사보고서를 찾지 못함 |
| `error` | 오류 | 보고서 탐색, 재무데이터 추출, XML 파싱 등에서 예외 발생 |

---

## 10. 프로젝트 구조

```
dart-ebitda-analyzer/
├── app.py                          # Streamlit UI (화면 렌더링, 결과 표시, Excel 생성)
├── pipeline.py                     # 파이프라인 오케스트레이션 (분석 흐름 제어)
├── config.py                       # 전역 설정 (추출 항목 정의, API 엔드포인트, 상수)
│
├── dart_api/                       # DART API 공통 모듈
│   ├── corp_search.py              #   기업코드 조회 및 캐싱
│   ├── report_finder.py            #   보고서 탐색 및 경로 결정
│   └── xml_utils.py                #   XML 다운로드/파싱 공유 유틸리티
│
├── financial/                      # 재무항목 추출 모듈
│   ├── api_extractor.py            #   경로A: DART 재무제표 API
│   └── doc_extractor.py            #   경로B: DART XML 문서 파싱
│
├── depreciation/                   # 감가상각 추출 모듈
│   └── extractor.py                #   CF/주석 기반 감가상각비·무형자산상각비 추출
│
├── ai_module/                      # AI 검증 모듈
│   └── gemini_parser.py            #   Gemini 기반 추출·비교·판정
│
├── validator/                      # 검증 모듈
│   └── rules.py                    #   회계 항등식 검증
│
├── utils/                          # 유틸리티
│   ├── cache.py                    #   SQLite 캐싱
│   └── analysis_logger.py          #   분석 로깅
│
├── prompts/                        # AI 프롬프트 가이드
│   └── depreciation_ai_guide.md    #   감가상각 추출 규칙 문서
│
├── fetch_raw_xml.py                # CLI: 감사보고서 XML 원문 다운로드 유틸
├── requirements.txt                # Python 의존성
└── .env.example                    # 환경변수 템플릿
```

---

## 11. 외부 의존성

### 11.1 외부 API

| API | 용도 | 인증 |
|-----|------|------|
| DART OpenAPI | 기업코드, 공시목록, 재무제표, 문서 원문 | `DART_API_KEY` |
| Google Gemini API | AI 추출·교차검증 | `GEMINI_API_KEY` |

### 11.2 Python 패키지

| 패키지 | 용도 |
|--------|------|
| streamlit | 웹 UI |
| pandas | DataFrame 처리 |
| openpyxl | Excel 생성 |
| requests | HTTP 클라이언트 |
| beautifulsoup4 | HTML/XML 파싱 |
| lxml | XML 처리 엔진 |
| google-genai | Gemini API 클라이언트 |
| python-dotenv | 환경변수 로드 |

---

## 12. 파이프라인 흐름도

```
[사용자 입력] 기업명 + 연도
        │
        ▼
[1] 기업코드 조회 (dart_api/corp_search)
        │ 미발견 → no_corp / 중복 → ambiguous_corp
        ▼
[2] 캐시 확인 (utils/cache)
        │ 히트 → 즉시 반환
        ▼
[3] 보고서 탐색 (dart_api/report_finder)
        │ 미발견 → no_report
        ├─ 사업보고서 → 경로A
        └─ 감사보고서 → 경로B
              │
        ┌─────┴─────┐
        ▼           ▼
[4A] 경로A        [4B] 경로B
DART API 호출    XML 다운로드+파싱
(financial/      (financial/
 api_extractor)   doc_extractor)
        │           │
        └─────┬─────┘
              ▼
[4-AI] AI 교차검증 (ai_module/gemini_parser)
       Python vs AI 비교 → 불일치 시 AI 판정
              │
              ▼
[4-B] 감가상각 전용 추출 (depreciation/extractor)
      CF 1차 → NOTES 2차 (AI 교차검증 포함)
              │
              ▼
[5] 회계 항등식 검증 (validator/rules)
              │
              ▼
[6] 캐시 저장 (utils/cache)
              │
              ▼
[결과 반환] → UI 표시 + Excel 다운로드
```
